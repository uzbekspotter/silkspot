# Dev journal (audit notes)

Опциональный дневник для крупных проходов по кодовой базе. Не дублирует CHANGELOG — только контекст «что смотрели и почему».

## 2026-04-11 — Убрать демо-данные, опереться на реальные фото

**Цель:** страницы продукта не должны показывать вымышленные цифры или «sample» там, где ожидаются данные из Supabase.

**Что проверено**

- **Explore / Profile / Stats / Community / About** — лента и профили уже тянут `photos` / `user_profiles`; явных DEMO-констант не найдено.
- **Upload / Aircraft / Photo detail** — плейсхолдеры в полях ввода оставлены (UX), не путаются с данными каталога.
- **Map** — раньше: запасной список `DEMO_AIRPORTS` и вымышленный `LIVE`. Теперь: только аэропорты из одобренных фото; боковая панель — последние одобренные загрузки из БД.
- **Fleet** — удалён массив `DEMO_AIRLINES` (~90 строк статического флота); список авиакомпаний строится только из одобренных снимков + справочник логотипов.
- **Admin / PhotoReviewTools** — убраны формулировки «demo» там, где это вводило в заблуждение; вкладка Duplicates честно говорит, что пайплайн не подключён.
- **`storage.ts` `source: 'demo'`** — технический fallback при отсутствии R2, не пользовательский «демо-контент»; оставлен.

**Файлы с правками в этом проходе:** `MapPage.tsx`, `FleetPage.tsx`, `PhotoReviewTools.tsx`, `AdminPage.tsx`, этот журнал, `session-handoff.md`.
